// No-op stub for the `server-only` package when running standalone scripts
// (integration tests) outside the Next.js build. Next.js resolves the real
// package during builds; this alias only applies under tsx --tsconfig.
export {};
